import { CompletedGatepassPipe } from './completed-gatepass.pipe';

describe('CompletedGatepassPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedGatepassPipe();
    expect(pipe).toBeTruthy();
  });
});
